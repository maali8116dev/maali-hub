# Local Supabase only (OAuth, etc.). Copy to .env.supabase — never commit.
# Production: Supabase Dashboard → Authentication → Providers → Google

SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=
